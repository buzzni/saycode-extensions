name: Release fixture

on:
  push:
    tags: ['v*']

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npm test
      - run: npm run typecheck
      - run: npm run verify:project-templates
      - run: npm run package:fixture
      - run: npm run package:project-templates
      - run: npm run package:plugin-manager
      - name: Create checksum
        working-directory: examples/hello-world
        run: sha256sum *.saycode-extension > buzzni.hello-world-1.0.0.saycode-extension.sha256
      - name: Create project templates checksum
        working-directory: packages/project-templates
        run: sha256sum *.saycode-extension > buzzni.project-templates-1.0.0.saycode-extension.sha256
      - name: Create Plugin Manager checksum
        working-directory: packages/plugin-manager
        run: sha256sum *.saycode-extension > buzzni.plugin-manager-1.0.2.saycode-extension.sha256
      - name: Publish GitHub release artifacts
        env:
          GH_TOKEN: ${{ github.token }}
        run: >-
          gh release create "$GITHUB_REF_NAME"
          examples/hello-world/buzzni.hello-world-1.0.0.saycode-extension
          examples/hello-world/buzzni.hello-world-1.0.0.saycode-extension.sha256
          packages/project-templates/buzzni.project-templates-1.0.0.saycode-extension
          packages/project-templates/buzzni.project-templates-1.0.0.saycode-extension.sha256
          packages/plugin-manager/buzzni.plugin-manager-1.0.2.saycode-extension
          packages/plugin-manager/buzzni.plugin-manager-1.0.2.saycode-extension.sha256
          --generate-notes
          --title "$GITHUB_REF_NAME"
